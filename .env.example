# Copy to .env — it is gitignored. Values containing # must be quoted.
ADMIN_PASSWORD="change-me"
SESSION_SECRET="run: openssl rand -hex 32"
PORT=3000

# Leave these unset for local development (SQLite file + uploads/ on disk).
# TURSO_DATABASE_URL="libsql://...turso.io"
# TURSO_AUTH_TOKEN=""
# BLOB_READ_WRITE_TOKEN=""
# SITE_URL="https://your-domain"
